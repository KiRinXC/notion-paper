# /paper-start

调用插件内置的 `notion-paper` Skill，并将本次调用严格路由为 `paper-start <PDF>`。

## 参数

- `pdf`：本次调用附带的论文 PDF，或用户提供的本地 PDF 路径（必需）。

## 约束

- 一篇论文对应一个 Codex 任务。
- 若未提供可唯一识别的 PDF，停止并要求用户补充；不要猜测。
- 只建立任务内论文上下文并在阅读年鉴中精确查重；条目不存在时不要自动初始化。
